# Bakery root environment
NODE_ENV=development

# Core credentials
DATABASE_URL=postgres://bakery:bakery@localhost:5432/bakery
SESSION_SECRET=changeme-session-secret
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# UI entrypoint (used for OAuth callbacks)
BAKERY_BASE_URL=http://localhost:5173

# Optional integrations
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Self-update webhook options (see README)
# Set `BAKERY_SELF_REPO` and `BAKERY_SELF_BRANCH` so the webhook knows which push to honor.
# `GITHUB_APP_WEBHOOK_SECRET` must match the GitHub App/webhook secret.
BAKERY_SELF_REPO=jevido/bakery
BAKERY_SELF_BRANCH=main
GITHUB_APP_WEBHOOK_SECRET=

# TLS automation (set when provisioning real certificates)
# CERTBOT_EMAIL=admin@example.com

# Uncomment to force/disable the lightweight local runtime (defaults to on in development)
# BAKERY_LOCAL_MODE=1
